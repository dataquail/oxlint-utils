{
  "name": "oxlint-architecture-rules",
  "$schema": "../../node_modules/nx/schemas/project-schema.json",
  "sourceRoot": "packages/oxlint-architecture-rules/src",
  "projectType": "library",
  "targets": {
    "build": {
      "executor": "nx:run-commands",
      "cache": true,
      "dependsOn": ["^build"],
      "inputs": ["production", "^production"],
      "outputs": ["{projectRoot}/build", "{projectRoot}/.tsbuildinfo"],
      "options": {
        "command": "pnpm run build",
        "cwd": "packages/oxlint-architecture-rules"
      }
    },
    "check": {
      "executor": "nx:run-commands",
      "cache": true,
      "outputs": ["{projectRoot}/.tsbuildinfo"],
      "options": {
        "command": "pnpm run check",
        "cwd": "packages/oxlint-architecture-rules"
      }
    },
    "test": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "pnpm run test",
        "cwd": "packages/oxlint-architecture-rules"
      }
    },
    "lint": {
      "executor": "nx:run-commands",
      "cache": true,
      "options": {
        "command": "pnpm exec oxlint --type-aware --quiet packages/oxlint-architecture-rules",
        "cwd": "{workspaceRoot}"
      }
    }
  },
  "tags": []
}
